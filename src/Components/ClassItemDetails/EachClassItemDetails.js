import React from 'react';

const EachClassItemDetails = (props) => {
    
    return (
        <div>
            {props.currentClass.id}
        </div>
    );
};

export default EachClassItemDetails;